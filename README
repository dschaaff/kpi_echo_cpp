kpi_echo_cpp

Description:
  Host will send a message to a kernel and wait for kernel's echo.
  Kernel will echo host's message back to the host.

Expected output:
  Host will print what was sent and what was received (in ascii)
  	printf("Sent: %s\n", sent);
  	printf("Received: %s\n", received);

    Build and run:
      1) Provide this git URL into KWS and click 'Run'.
